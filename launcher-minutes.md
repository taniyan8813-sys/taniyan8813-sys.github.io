# ランチャー議事録

## 2026-05-14

### 変更内容
- `GLITCH STORY` の共通入口を作成。
- `GLITCH E-01 : RECODE` と `AKARI : RESTART` を選んで起動できるランチャー画面を追加。
- PCブラウザとスマホ横画面を想定し、横長画面では1画面内に収まるよう調整。
- タイトルは英表記のまま維持。
- HUDやゲーム内ログは各プロトタイプ側の日本語表示を維持。
- ランチャーでは、今は入れない要素も明記した。
  - 第二章のAI正体
  - 観測者の正体
  - E-02以降
  - 物語の核心説明

### 次に検証したいこと
- 共通入口から2本のゲームへ自然に入れるか。
- 将来的にここへ「説明」「操作方法」「更新履歴」を足すか。

### GitHub Pages配置
- `taniyan8813-sys.github.io` リポジトリにランチャーと2本のプロトタイプを配置。
- 公開ルートは `GLITCH STORY` の選択画面に変更。
- `mode-prototype/` に `GLITCH E-01 : RECODE` を配置。
- `survival-prototype/` に `AKARI : RESTART` を配置。
- `ads.txt` は既存のまま維持。
- 既存ページにあった canonical、description、AdSenseスクリプトをランチャー側へ継承。
- ローカル確認用サーバーでランチャー、GLITCH、AKARIの表示とコンソールエラーなしを確認。
